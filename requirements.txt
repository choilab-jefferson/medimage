# Core toolkit for Chapters 1-4
imageio>=2.37
pydicom>=3.0
numpy>=2.0
scipy>=1.14
matplotlib>=3.9
pandas>=2.2
scikit-image>=0.24
torch>=2.4

# Chapters 5-6
SimpleITK>=2.4
qradiomics>=0.9.5
# The default extraction engine. It is an optional extra of qradiomics rather
# than a dependency, because PyPI has no pyradiomics wheel for Python 3.10+ and
# its 3.1.0 sdist has broken metadata. Installing from upstream git works.
pyradiomics @ git+https://github.com/AIM-Harvard/pyradiomics.git
lifelines>=0.30
remotezip>=0.12

# Chapters 10-13 (feature extraction, registration, delta, classification)
statsmodels>=0.14
scikit-learn>=1.5
lightgbm>=4.5
xgboost>=2.1
opencv-python-headless>=4.10
rt-utils>=1.2
